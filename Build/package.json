{
  "name": "moinferdi-chatbot-build",
  "private": true,
  "description": "Build tooling for the chatbot widget's TypeScript sources. Not published.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.6.0"
  }
}
